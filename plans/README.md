# Implementation Plans

Generated by the improve skill on 2026-06-12, against commit `262c1cc`. Execute
in the order below unless dependencies say otherwise. Each executor: read the
plan fully before starting, honor its STOP conditions, and update your row when
done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001  | Fix semantic-release branch + CI gate | P1 | S | — | DEFERRED — user opted out of GitHub CI (2026-06-15) |
| 002  | Upgrade Strapi 5.33.3 → 5.48.0 (clears 11 critical CVEs) | P1 | M | 001 (soft) | DONE (2026-06-15) — see execution log |
| 003  | Harden backup cron (stream, async, encrypt, BACKUP_ENABLED) | P2 | M | — | DEFERRED — user excluded backup work this round (2026-06-15) |
| 004  | users-permissions hardening (policy returns, role coupling doc, drop lodash) | P2 | S | — | DONE (2026-06-15) |
| 005  | Test baseline (helper + isOwnerOrAdmin unit tests) | P2 | M | 004 (preferred) | DONE (2026-06-15) |
| 006  | Template polish (generate-keys.sh, .editorconfig yml, README checklist) | P3 | S | — | DONE (2026-06-15) |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale) | DEFERRED (with one-line reason)

## Execution log — 2026-06-15

Executed directly on `master` (single branch, nothing to consolidate). Plans 002, 004, 005, 006 applied; 001 and 003 deferred per user instruction ("on va faire tout le reste sauf la backup", "pas de CI github").

- **002 — DONE with caveat.** `@strapi/upgrade minor` bumped the four `@strapi/*` packages 5.33.3 → 5.48.0 (latest). `tsc --noEmit`, `npm run build`, `vp check` all green. `npm audit --omit=dev` went from **77 vulns (11 critical / 21 high)** to **44 (1 critical / 22 high)**. The residual critical (`shell-quote`) and most highs are transitive **inside Strapi 5.48.0's own tree** (`@strapi/design-system` → lodash/lodash-es, `esbuild`, `rollup`, `fast-uri`, `picomatch`, `minimatch`, `jws`) — 5.48.0 is the newest published release, so only upstream Strapi can clear them. `npm audit fix --force` was NOT run (it would break the Strapi pin — explicitly out of scope). The plan's "0 critical / 0 high" criterion is therefore not fully met; net result is a large reduction and the latest available Strapi.
- **004 — DONE.** `isOwnerOrAdmin` now returns `false` on denial (not `ctx.unauthorized()`); `allowedFields:['role']` ↔ `sanitizeUser` coupling documented in both files; `require('lodash')` and `Math.random()` removed from `helper.ts` (crypto `randomBytes` username, pure `toPathSegments`).
- **005 — DONE.** `tests/extensions/users-permissions/{helper,strapi-server}.test.ts` — 12 tests, green under both `vp test` (Vitest) and `npm test` (bun).
- **006 — DONE.** `generate-keys.sh` rewritten POSIX-clean, no secret echo, `APP_KEYS` as 4 comma-separated keys (verified in scratch run); `.editorconfig` no longer forces tabs on YAML; README gained a "New project checklist".

## Dependency notes

- 002 after 001 (soft): fix the release pipeline before an upgrade commit could trigger a release from a broken config. Not blocking.
- 005 after 004 (preferred): the tests target the post-004 helper shape (crypto username, no lodash). Plan 005 documents how to run before 004 if needed.
- 003 and 006 interact on one line: plan 003 replaces the hardcoded backup prefix in `src/index.ts` with `BACKUP_FILE_PREFIX`; plan 006's README checklist words that item conditionally. Either order works.
- 002 may codemod source files — run its drift checks before 003/004 if 002 lands first.

## Findings considered and rejected

- **contentmanageruser.update allows `confirmed`/`blocked`** (`strapi-server.ts:5,72-81`): by design — the content-manager route is admin-authenticated, and admins legitimately manage confirmation/blocking. The user-facing `user.update` route blocks all four fields.
- **React 18 outdated**: correct as-is — Strapi 5.x peer-depends on React 18; bumping to 19 would break the admin build. Revisit only when Strapi's peer range moves.
- **`register.allowedFields: ['role']` as a standalone critical vuln**: downgraded — neutralized by the `auth.register` override that rebuilds the body (`sanitizeUser`). The residual risk (fragile coupling) is handled by documentation + tests in plans 004/005, not by removing the field (the override requires it).
- **`fs.readFileSync`/`execSync` reported twice** (correctness + perf categories): merged into plan 003.
- **`require('lodash')` reported twice** (correctness + tech-debt): merged into plan 004.
- **S3Client constructed per cron run**: folded into plan 003 (step 5), not a standalone finding.
- **Duplicate favicon / `*.csv` gitignore / empty `strapi.uuid`**: documentation-only — folded into plan 006's checklist.
